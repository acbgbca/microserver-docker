grafana:
  services:
  - name: grafana
    image: grafana/grafana-oss:12.4.0@sha256:b0ae311af06228bcfd4a620504b653db80f5b91e94dc3dc2a5b7dab202bcde20
    exposed:
      internal: true
      external: true
      port: 3000
    env:
      GF_AUTH_PROXY_ENABLED: 'true'
      GF_AUTH_PROXY_HEADER_NAME: REMOTE-USER
      GF_AUTH_PROXY_HEADERS: Name:REMOTE-NAME Email:REMOTE-EMAIL Groups:REMOTE-GROUPS
      GF_LOG_LEVEL: warn # Grafana logs way too much at info level
    user: '{{ userId }}:{{ groupId }}'
    volumes:
      - local: data
        container: /var/lib/grafana
  - name: alloy
    image: grafana/alloy:v1.13.2@sha256:8abcd0d97a97099cabb34be8dcfc448fca6c35931ae9291aaf74056105dedcca
    command: run --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
    volumes:
      - file: config.alloy
        container: /etc/alloy/config.alloy
      - local: alloy
        container: /var/lib/alloy/data
      - filesystem: /var/lib/docker/containers
        container: /var/lib/docker/containers
        permissions: ro
      - filesystem: /var/run/docker.sock
        container: /var/run/docker.sock
      - filesystem: /var/log
        container: /logs
        permissions: ro
