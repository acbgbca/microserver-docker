services:
  ptv:
    image: ghcr.io/acbgbca/ptv-monitor:latest
    container_name: ptv
    user: 9001:9001
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.services.ptv.loadbalancer.server.port=8080
      - show.external=true
    volumes:
      - ./config.yaml:/config/config.yaml:ro
      - ./data:/data

networks:
  default:
    external: true
    name: ctr-network
