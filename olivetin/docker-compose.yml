services:
  olivetin:
    container_name: olivetin
    image: ghcr.io/olivetin/olivetin:2025.11.11@sha256:6e679bcee272b6db69a56fe0b386feaecc0170f3319016ec48a8ce26ee97d448
    labels:
      - traefik.enable=true
      - traefik.http.services.olivetin.loadbalancer.server.port=1337
      - show.external=true
    volumes:
      - ./config:/config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
networks:
  default:
    external: true
    name: ctr-network