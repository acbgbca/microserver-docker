services:
  olivetin:
    container_name: olivetin
    image: ghcr.io/olivetin/olivetin:2025.6.1@sha256:17f7ed5817c8771b2df186ee68e7d6be904ff4c2e118640bbcff0b418456ce11
    labels:
      - traefik.enable=true
      - traefik.http.services.olivetin.loadbalancer.server.port=1337
      - show.external=true
    volumes:
      - ./config:/config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
networks:
  default:
    external: true
    name: ctr-network